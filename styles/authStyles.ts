import {StyleSheet} from 'react-native';

export const authStyles = StyleSheet.create({
  backgroundImage: {
    flex: 1,
    justifyContent: 'center',
  },
  wrapper: {
    margin: 20,
    padding: 20,
    borderRadius: 10,
    elevation: 4,
  },
  authTitle: {
    marginBottom: 20,
    textAlign: 'center',
  },
  input: {
    marginBottom: 15,
  },
});
